export const metadata = { title: "thomas • API OTP" };
export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body style={{ padding: 20, fontFamily: "system-ui" }}>{children}</body>
    </html>
  );
}
